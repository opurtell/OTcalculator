node_modules/
dist/
.storybook-static/
*.local
.DS_Store

# design-sync: staged scripts, regenerated output, machine state.
# Everything else under .design-sync/ (config.json, NOTES.md, conventions.md,
# previews/, overrides/) is durable and IS committed.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
# Regenerated from the live bundle immediately before every upload.
.design-sync/upload-manifest.txt
.design-sync/upload-batch.json
